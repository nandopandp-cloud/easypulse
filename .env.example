# Postgres (Neon, Supabase, Railway, etc.)
DATABASE_URL="postgresql://user:password@host:5432/easypulse?sslmode=require"

# JWT secret for session signing — generate with: openssl rand -base64 48
AUTH_SECRET="change-me-to-a-long-random-string"
